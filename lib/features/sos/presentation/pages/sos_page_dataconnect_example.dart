import 'package:flutter/material.dart';

// Placeholder page retained to avoid route breakage. Original DataConnect example
// removed to eliminate references to deprecated services and reduce build size.
class SOSPageDataConnectExample extends StatelessWidget {
  const SOSPageDataConnectExample({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'DataConnect example removed.\nThis page is a placeholder.',
          textAlign: TextAlign.center,
        ),
      ),
    );
  }
}
