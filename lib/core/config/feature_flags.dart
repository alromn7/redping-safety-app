/// Centralized feature flags for runtime/build-time gating.
/// Set Safety Fund to false to fully disable related UI and services.
class FeatureFlags {
  static const bool enableSafetyFund = false;
}
