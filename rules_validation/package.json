{
  "name": "rules-validation",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.6",
    "firebase-tools": "^13.0.1"
  },
  "scripts": {
    "test": "node tests/run.js",
    "validate": "npx firebase emulators:exec \"node tests/run.js\" --only firestore --project redping-staging --quiet"
  }
}
