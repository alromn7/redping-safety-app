name: shared_core
description: Shared core skeleton for RedPing apps (planning)
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  meta: ^1.11.0

dev_dependencies:
  lints: ^2.1.0
